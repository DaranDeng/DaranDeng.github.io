# 滑动窗口大法好
+ 所谓滑动窗口，就是不断的调节数组子序列的起始位置和终止位置，从而得出我们想要的结果。
+ 终止位置指针：就是for循环中的变量j。
+ 起始位置指针：精髓在于怎么调节起始位置i。满足某个特定条件，i才动。还要思考这个i的移动会不会带来这个滑动窗口的值的变化，比如窗口内所有元素之和。
+ plus：`return res == INT_MAX ?0:res;`这个三元运算是return在前。注意最好括号括起来，因为三元运算符优先级较低。
