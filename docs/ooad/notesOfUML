## 用例图
![alt text](image.png)
- 泛化就是继承，表示子类拥有父类的所有特征和行为，并且可以有自己的特有行为。那为什么参与者之间可以有泛化关系？比如员工和经理，经理继承员工，经理是员工，员工能做的他都能做，而且还能做一些普通员工做不了的事。
- 基用例VS子用例：子用例不能脱离基用例独立存在。
- A包含B表示，A跟B似乎是有个先后顺序。这个不同于包含，B必须有。
## 类图
![alt text](image-1.png)
![alt text](image-2.png)
![alt text](image-3.png)
- 看名词性，关联有三种。
## 顺序图
纵轴表示时间，往下表示时间流逝；横轴表示类的交互。
-   **边界类**：负责**对外交互**。比如：窗口、对话框、API接口、传感器等。  
    （作用是：把外部世界和内部逻辑隔开）

-   **控制类**：负责**业务逻辑**。比如：处理订单、计算价格、执行用例的流程。  
    （作用是：像个“工头”，指挥实体类干活）

-   **实体类**：负责**存数据**。比如：用户信息、商品数据、数据库表。  
    （作用是：只管存和取，不管怎么用）
    