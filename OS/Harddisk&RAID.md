error correcting code is expensive because detect errors itself and correct them
SSD solid state disk can even hold data 开关机电脑的时候 
why redundant
RAID 0 
如果文件过小，由于不能分为几个块，RAID 0起不到好的效果
bit level
byte level ，如果只有一个disk出错，可以通过额外的磁盘纠错。
block level